server:
  # encoding setting
  servlet:
    encoding:
      force-response: true
    session:
      tracking-modes: cookie
spring:
  # console log color
  output:
    ansi:
      enabled: always
  # devtools setting
  devtools:
    restart:
      enabled: true
  # mustache
  mustache:
    cache: false
    expose-session-attributes: true
  # h2 DB setting
  #  h2:
  #    datasource:
  #      url: jdbc:h2:mem:testdb
  #      driver-class-name: org.h2.Driver
  #      username: sa
  #      password:
  #    console:
  #      enabled: true
  #      path: /h2-console

  # datasource
  datasource:
    url: jdbc:mysql://localhost:3306/spring_cafe
    username: user01
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
    generate-unique-name: false
  # mvc
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  
  sql:
    init:
      mode: always
