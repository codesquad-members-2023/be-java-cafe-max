spring:
  profiles:
    active: prod
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      mode: always
  mustache:
    suffix: .html
    servlet:
      cache: false
server:
  servlet:
    encoding:
      force: true
